/**
* Broadcast data changes on the element
*/

class Datasource extends Component{
	constructor(el, opts){
		var self = super(el, opts);
		return self;
	}


}

Datasource.states = {
	default: {

	}
}

Datasource.defaults = {

}

Component.register(Datasource)